# Default values for springboot-demo.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: springboot-demo
  tag: 0.0.1-SNAPSHOT
  pullPolicy: Never

nameOverride: ""
fullnameOverride: ""

service:
  type: LoadBalancer
  port: 8088

ingress:
  enabled: false
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  path: /
  hosts:
    - chart-example.local
  tls: []

app:
  port: 8080

demo:
  name: James Bond
  greeting: Salut